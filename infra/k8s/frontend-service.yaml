apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: fullstack-platform
  labels:
    app: frontend
    tier: frontend
spec:
  selector:
    app: frontend
    tier: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  type: ClusterIP
