apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: fullstack-platform
  labels:
    app: auth-service
    tier: backend
spec:
  selector:
    app: auth-service
    tier: backend
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  type: ClusterIP
