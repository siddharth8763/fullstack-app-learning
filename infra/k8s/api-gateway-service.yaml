apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: fullstack-platform
  labels:
    app: api-gateway
    tier: backend
spec:
  selector:
    app: api-gateway
    tier: backend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
